services:
  - type: web
    name: stellar-bot
    env: docker  # ← КЛЮЧЕВОЕ ИЗМЕНЕНИЕ!
    plan: free
    region: oregon
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: ADMIN_IDS
        sync: false
